#include <stdio.h>

/**
 * main - Lanzador
 *
 * Return: Always return (0)
 */

int main(void)
{
	int digits = 0;

	for (digits = 0; digits < 10; digits++)
	{
		putchar(digits);
	}
	putchar('\n');
	return (0);
}
